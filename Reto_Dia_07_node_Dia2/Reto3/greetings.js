/* RETO 3
1. Crea una carpeta y genera un paquete llamado codenotch-node.
2. Dentro crea dos módulos uno con una función greeting que dado un nombre imprima por consola un saludo y otro que use el módulo anterior. */

const exp = require ('./export')

console.log(exp.saludo("José"))